# =================================================================
# GLOBAL CONFIGURATION
# These variables are shared across all services
# =================================================================

# --- General ---
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# --- Database ---
# Used by main-service, medias-service, role-permission-gateway
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=maindb

# Medias Service Database
MEDIAS_DB_HOST=medias-db
MEDIAS_DB_USERNAME=postgres
MEDIAS_DB_PASSWORD=postgres
MEDIAS_DB_NAME=mediasdb

# --- JWT ---
JWT_SECRET=thangpham
JWT_ACCESS_TOKEN_EXPIRES_IN=3600s
JWT_EXPIRES_IN=24h
SALT_ROUNDS=10

# --- RabbitMQ ---
RABBITMQ_URL=amqp://admin:admin123@rabbitmq:5672
RABBITMQ_EXCHANGE=events

# --- GrowthBook ---
GROWTHBOOK_API_HOST=https://cdn.growthbook.io
GROWTHBOOK_CLIENT_KEY=sdk-EPaCv1Y3IXYGr
GROWTHBOOK_TENANT_ID=123

# --- Admin User ---
# Used by main-service to create a super admin
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# --- Service Ports ---
MAIN_SERVICE_PORT=3000
AUTH_SERVICE_PORT=3002
MEDIAS_SERVICE_PORT=3001
USER_PERMISSION_GATEWAY_PORT=3004

# --- Database Ports ---
DB_PORT_EXTERNAL=5432
DB_PORT_INTERNAL=5432
MEDIAS_DB_PORT_EXTERNAL=5433
MEDIAS_DB_PORT_INTERNAL=5432

# --- RabbitMQ Ports ---
RABBITMQ_PORT_EXTERNAL=5672
RABBITMQ_PORT_INTERNAL=5672
RABBITMQ_MANAGEMENT_PORT_EXTERNAL=15672
RABBITMQ_MANAGEMENT_PORT_INTERNAL=15672

# --- Service URLs ---
# These are the URLs for inter-service communication
MAIN_SERVICE_URL=http://main-service:${MAIN_SERVICE_PORT}
AUTH_SERVICE_URL=http://auth-service:${AUTH_SERVICE_PORT}
MEDIAS_SERVICE_URL=http://medias-service:${MEDIAS_SERVICE_PORT}
USER_PERMISSION_GATEWAY_URL=http://user-permission-gateway:${USER_PERMISSION_GATEWAY_PORT}